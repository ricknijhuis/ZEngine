pub const WindowResizeEvent = struct {
    width: u32,
    height: u32,
};

pub const WindowMaximizeEvent = struct {
    width: u32,
    height: u32,
};
pub const WindowMinimizeEvent = struct {};
pub const WindowCloseEvent = struct {};
